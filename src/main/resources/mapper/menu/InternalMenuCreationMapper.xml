<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.deliveryinsider.store.domain.menu.mapper.InternalMenuCreationMapper">

    <insert id="insertPending">
        INSERT INTO internal_menu_creation_operations (operation_key, store_id)
        VALUES (#{operationKey}, #{storeId})
        ON DUPLICATE KEY UPDATE operation_key = VALUES(operation_key)
    </insert>

    <select id="findMenuIdForUpdate" resultType="java.lang.Long">
        SELECT menu_id
        FROM internal_menu_creation_operations
        WHERE operation_key = #{operationKey}
          AND store_id = #{storeId}
        FOR UPDATE
    </select>

    <update id="complete">
        UPDATE internal_menu_creation_operations
        SET menu_id = #{menuId}, completed_at = CURRENT_TIMESTAMP(6)
        WHERE operation_key = #{operationKey}
          AND store_id = #{storeId}
          AND menu_id IS NULL
    </update>
</mapper>
